import {Font} from '../../labels/typography';
import {typographyValues} from '../../values/TypographyValues';

const font: Record<Font, string> = {
    [Font.BASE]: typographyValues.FONT_BASE,
};

export {font};
